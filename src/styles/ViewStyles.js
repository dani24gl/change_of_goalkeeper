import { StyleSheet } from "react-native";

const viewStyles = StyleSheet.create({
  container: {
    alignItems: 'center',
    backgroundColor: '#a8cfe6',
    flex: 1,
    justifyContent: 'center',
  },
});


export default viewStyles;
